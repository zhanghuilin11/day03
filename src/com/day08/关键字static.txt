使用范围：
    在Java类中，可以用static修饰属性，方法，代码块，内部类
被修饰的成员由以下特点：
    随着类的加载二加载
    优先于对象存在
    修饰的成员，被左右对象所共享
    访问权限允许时，可不创建对象，直接被类调用


类变量：这种可以被所有实例化对象共享的属性，使用起来

类方法：同类变量
类方法因为不需要实例就可以访问static方法，因此static方法内部不能使用
this、super、yes等
重载的方法必须为同时static或者非static的