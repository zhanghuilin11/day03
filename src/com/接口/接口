有时不许从几个类中派生出一个子类，继承他们所有的属性和方法。但是，Java不支持多继承，
有了接口，就可以得到多车哦那个继承的效果
接口（interface）时抽象方法和常量值的定义的集合
从本质上讲，就扣是一种特殊的抽象类，这种抽象类中只包含常量和方法的定义，而没有变量和方法的实现
实现接口类：
class SubClass implements Interface{}
一个类可以实现多个接口，接口也可以继承其他接口

接口的特点：
    用interface来定义
    接口中所有成员变量都默认由 public static final 修饰的
    接口中所有方法都时默认由public abstract 修饰的
    接口没有构造器
    接口采用多层继承机制

 实现接口的类必须提供接口中所有方法的具体实现，方可实例化，否则这个类仍为抽象类（接口是特殊的抽象类）
 接口的主要用途就是被实现类实现（面向接口编程）
 与继承关系类似，接口与实现类之间存在多态性
 定义Java类的语法格式是先写extends后写implements

 接口可以继承接口